# {{ project_name }}

{{ project_description }}

## Project Organization

```plaintext
{{ project_slug }}/
├── LICENSE            <- Open-source license if one is chosen
├── README.md          <- The top-level README for developers using this project.
├── properdocs.yml     <- ProperDocs (mkdocs-material theme) configuration file.
├── pyproject.toml     <- Project configuration file with package metadata for
│                         {{ project_slug }} and configuration for tools like ruff
├── uv.lock            <- The lock file for reproducing the production environment, e.g.
│                         generated with `uv sync`
├── scripts            <- Release and helper scripts.
├── docs               <- A default ProperDocs project; see properdocs.org for details
├── data
│   ├── external       <- Data from third party sources.
│   ├── interim        <- Intermediate data that has been transformed.
│   ├── processed      <- The final, canonical data sets for modeling.
│   └── raw            <- The original, immutable data dump.
├── models             <- Trained and serialized models, model predictions, or model summaries
├── notebooks          <- Jupyter notebooks. Naming convention is a number (for ordering),
│                         the creator's initials, and a short `-` delimited description, e.g.
│                         `1.0-jqp-initial-data-exploration`.
├── references         <- Data dictionaries, manuals, and all other explanatory materials.
├── reports            <- Generated analysis as HTML, PDF, LaTeX, etc.
│   └── figures        <- Generated graphics and figures to be used in reporting
├── tests              <- Unit test files.
{%- if create_examples %}
├── Dockerfile.package <- Builds a production image from the built package.
├── Dockerfile.source  <- Builds a production image from source.
├── docker-compose.yml <- Runs the FastAPI app locally via Docker.
├── certs              <- Corporate CA certificates trusted at build time.
{%- endif %}
└── src/{{ project_slug }}   <- Source code for use in this project.
    │
    ├── __init__.py             <- Makes {{ project_slug }} a Python module
    ├── _version.py             <- Single source of the package version
    ├── cli.py                  <- Default CLI program
{%- if create_examples %}
    ├── api/                    <- FastAPI application routes and settings
    ├── app.py                  <- FastAPI application entrypoint
    ├── nlp.py                  <- NLP semantic-search example
    ├── tabular.py              <- Tabular data training example
    └── vision.py               <- Vision training example
{%- endif %}
```

## For Developers

### Whether to use `package`

This determines if the project should be treated as a Python package or a "virtual" project.

A `package` is a fully installable Python module,
while a virtual project is not installable but manages its dependencies in the virtual environment.

If you don't want to use this packaging feature,
you can set `tool.uv.package = false` in the pyproject.toml file.
This tells `uv` to handle your project as a virtual project instead of a package.

### Install Python (3.14)
```shell
uv python install 3.14
```

### Pin Python version
```shell
uv python pin 3.14
```

### Install packages with PyTorch + CUDA 13.0 (Ubuntu)
```shell
uv sync --extra cu130
```

### Install packages without locking environments
```shell
uv sync --frozen
```

### Install dev packages, too
```shell
uv sync --group dev --group docs --extra cu130
```

### Run tests
```shell
uv run pytest
```

### Linting
```shell
uv run ruff check --fix .
```

### Formatting
```shell
uv run ruff format
```

### Run pre-commit
* Assume that `pre-commit` installed with `uv tool install pre-commit`

```shell
uvx pre-commit run --all-files
```

### Build package
```shell
uv build
```

### Serve Document
```shell
uv run properdocs serve
```

### Build Document
```shell
uv run properdocs build
```

{% if create_examples %}
### Build Docker Image (from source)

[ref. uv docs](https://docs.astral.sh/uv/guides/integration/docker/#installing-a-project)

```shell
docker build -t TAGNAME -f Dockerfile.source
```

### Build Docker Image (from package)

[ref. uv docs](https://docs.astral.sh/uv/guides/integration/docker/#non-editable-installs)

```shell
docker build -t TAGNAME -f Dockerfile.package
```

### Run Docker Container
```shell
docker run -p 8000:5000 {{ project_slug }}:{{ version }}
```

GPU inference needs a CUDA-enabled base image; build with the `cu126`/`cu130`/`cu132` extra baked in and add `--gpus all` to the `docker run` command above.

{% endif %}
### Check next version
```shell
uv run git-cliff --bumped-version
```

### Release
Execute scripts
```shell
sh scripts/release.sh
```

What `release.sh` does:

1. Read the current version with `uv version --short` and compute the next
   version with `uv run git-cliff --bumped-version`.
2. Set the new version with `uv version`, then regenerate `CHANGELOG.md` and
   `RELEASE.md` with `git-cliff`.
3. Update the lock file with `uv lock`.
4. Commit `pyproject.toml`, `CHANGELOG.md`, `RELEASE.md`, `uv.lock` (and
   `__init__.py`/`tests/test_version.py` if present) as
   `chore(release): bump version to X`, push, tag `vX`, and push the tag.

## References
* [Packaging Python Projects](https://packaging.python.org/tutorials/packaging-projects/)
* [Python Packaging User Guide](https://packaging.python.org/)

This project template is generated by [copier-modern-ml](https://github.com/appleparan/copier-modern-ml).

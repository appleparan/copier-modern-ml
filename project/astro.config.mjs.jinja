// @ts-check
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import mermaid from 'astro-mermaid';
import rehypeKatex from 'rehype-katex';
import remarkMath from 'remark-math';

export default defineConfig({
  // Keep the docs site out of the Python `src/` tree.
  srcDir: './docs',
  // Astro's default `dist/` is where `uv build` writes wheels.
  outDir: './site',
  integrations: [
    mermaid(),
    starlight({
      title: {{ project_name | tojson }},
      description: {{ project_description | tojson }},
      social: [
        { icon: {% if ci == 'gitlab' %}'gitlab'{% else %}'github'{% endif %}, label: 'Repository', href: {{ repo_url | tojson }} },
      ],
      customCss: ['katex/dist/katex.min.css'],
      sidebar: [
        { label: 'Home', link: '/' },
        { label: 'Overview', link: '/overview/' },
        { label: 'Getting Started', link: '/getting-started/' },
        // Generated by `scripts/gen_ref_pages.py` (run via `make docs`).
        { label: 'API Reference', items: [{ autogenerate: { directory: 'reference' } }] },
      ],
    }),
  ],
  markdown: {
    remarkPlugins: [remarkMath],
    rehypePlugins: [rehypeKatex],
  },
});
